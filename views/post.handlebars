<div class="text-left">
  <h2>{{post.title}}, Created By: {{post.userId}}</h2>
  <p>{{post.content}}</p>
  
  </br>
  <h3>Comments Section</h3>
  
  {{#each comments as |comment| }}
    <p>{{ comment.userId }} says: {{ comment.content }}</p>
  {{/each}}

</div>
